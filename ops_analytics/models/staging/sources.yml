version: 2

sources:
  - name: raw
    schema: default
    tables:
      - name: orders_raw
        description: "Raw orders, shipping, and customer data (DataCo Smart Supply Chain), 1 row = 1 order item"